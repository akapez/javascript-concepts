//isArray: Array.isArray(value). It returns true if the value is an array, and false otherwise.

const countries = ['USA', 'Sri Lanka', 'Australia'];
const str = 'Hello';
const numb = 3;


console.log(Array.isArray(str));